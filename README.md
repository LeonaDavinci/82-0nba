# 82-0nba

> 项目工程脚手架（尚未绑定具体技术栈）。

本仓库目前仅包含基础工程结构与约定，后续可按需填充具体框架与代码。

## 目录结构约定

```
82-0nba/
├── docs/        # 文档、设计说明、会议纪要等
├── src/         # 源码主目录（按模块/功能组织）
├── tests/       # 测试代码
├── scripts/     # 构建、部署、自动化脚本
├── .gitignore   # 忽略规则
└── README.md    # 项目说明（本文件）
```

> 说明：以上目录仅为约定占位。引入具体框架（如 React、Vue、Node、Python 等）后，
> 可在此处补充对应的目录说明与构建/运行命令。

## 本地开发

仓库暂未绑定框架，暂无统一构建/运行命令。
确定技术栈后，请补充 `package.json` / `pyproject.toml` / `Makefile` 等入口文件，
并在本 README 中记录安装与启动步骤。

## 提交规范（建议）

- `feat:` 新功能
- `fix:` 缺陷修复
- `docs:` 文档变更
- `chore:` 构建/工具变动
- `refactor:` 重构

示例：`git commit -m "feat: 初始化用户登录模块"`
